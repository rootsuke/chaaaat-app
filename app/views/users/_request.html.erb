<%= form_with(model: current_user.friend_requests.build) do |f| %>
  <%= hidden_field_tag(:reciever_id, @user.id) %>
  <%= f.submit "Send Friend Request", class: "btn btn-success", id: "request-btn" %>
<% end %>
